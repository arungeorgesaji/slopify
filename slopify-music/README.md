# slopify

API reference files at the repo root:

- `postman_collection.json`: ready-to-import Postman collection for the current endpoints
- `response_formats.json`: example success and error response bodies for each endpoint
